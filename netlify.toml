[build]
  command = "npm ci --include=dev && npm run build"
  publish = "dist/portfolio/browser"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--include=dev"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
